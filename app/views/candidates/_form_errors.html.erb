<% if object.errors.any? %>
  <div id='error_explanation'>
    <p><%= pluralize(object.errors.count, 'error') %> prohibited this user from being submitted:</p>

    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
